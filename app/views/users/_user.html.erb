<li>
  <tr>
    <td>
      <%= link_to user.name, user %>
    </td>
      <td>
        <% if current_user.admin? %>
          <%= link_to "edit", edit_user_path(user) %> | 
          <%= link_to "delete", user, method: :delete,
                                    data: { confirm: "This cannot be undone, are you sure you want to delete this user?" } %>
        <% end %>
      </td>
  </tr>
</li>